---
Before Day-1
---
I already knew basic Python programming, essential Git commands, and foundational mathematical concepts like linear algebra and vector calculus. I also had experience building web applications using Python, Streamlit, and the MERN stack.

---
## Day-1 Checklist
- [x] I can open a Linux terminal and recognize the shell prompt (`user@machine:~$`)
- [x] I can run `python --version`, `git --version`, and `uv --version` without errors
- [x] I can open VS Code connected to WSL/Linux using `code .`
- [x] I know what `~` means and can navigate to my home directory using `cd ~`
- [x] I can create folders and files using `mkdir` and `touch`, and list them with `ls`
- [x] I know the difference between `.xyz`, `./xyz`, and `../xyz`
- [x] I know that in Linux, everything is a file
- [x] I can navigate to the C and D drives within WSL
- [x] I understand the difference between `>` (overwrite) and `>>` (append)
- [x] I have a GitHub account and have created the `tds-bootcamp` repository

---
After Day-1
---
I learned these things as well, apart from the checklist:
* Efficient environment management workflows using `uv`.
* Navigating the Windows file system seamlessly through the `/mnt/c/` and `/mnt/d/` directories within WSL.

------
Feedback (Suggestions for the TDS Team)
---
The environment setup instructions were clear and straightforward. Looking forward to diving deeper into data science pipelines and advanced workflows in the coming days!

---------
### Personal Notes
* Use `cd ~` or just `cd` to quickly head back to the Linux home directory.
* Remember to use `>>` when logging or appending data so I don't accidentally wipe out an existing file with `>`.
